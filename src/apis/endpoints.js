// live giveaway endpoints
const total_giveaway = '/worth';
const giveaway_list = '/giveaways';
const endpoints = {
  // live giveaway endpoints
  total_giveaway,
  giveaway_list,
};

export default endpoints;
